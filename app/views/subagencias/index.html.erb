<% provide(:title, "Listado Subagencias") %>
<div id="wrapper">
  <%= render 'agencias/navigation' %>
  <div id="page-wrapper">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="page-header">Listado</h1>
        <% @subagencias.each do |s|%>
          <p>Titular:
            <%= s.titular %></p>
        <% end %>
      </div>
      <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /#page-wrapper -->
</div>
